{
  "name": "@teqbench/tbx-mat-bottom-sheets",
  "version": "3.0.4",
  "description": "Opinionated Angular bottom sheet service built on Material's MatBottomSheet. Severity-leveled methods (success / error / warning / information / help / default) plus dialog-style UX patterns (confirm / input). Returns Promise<TbxMatBottomSheetResult<T, F>>. Severity colors via @teqbench/tbx-mat-severity-theme; pluggable icons via TBX_MAT_BOTTOM_SHEET_PROVIDER_CONFIG. Angular 21+.",
  "type": "module",
  "exports": {
    "./styles/*": {
      "sass": "./styles/_*.scss"
    }
  },
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=24.0.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "ng-packagr -p ng-package.json -c tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "storybook": "STORYBOOK_MODE=dev storybook dev -p 6006 -c .storybook",
    "storybook:dev": "STORYBOOK_MODE=dev storybook dev -p 6006 -c .storybook",
    "storybook:docs": "STORYBOOK_MODE=docs storybook dev -p 6007 -c .storybook",
    "build-storybook": "STORYBOOK_MODE=dev storybook build -c .storybook -o storybook-dev-static",
    "build-storybook:dev": "STORYBOOK_MODE=dev storybook build -c .storybook -o storybook-dev-static",
    "build-storybook:docs": "STORYBOOK_MODE=docs storybook build -c .storybook -o storybook-docs-static"
  },
  "devDependenciesPinned": {
    "//": [
      "typescript-eslint is pinned without ^ because patch releases have historically introduced",
      "breaking rule changes. Pin to a known-good version and update deliberately after testing.",
      "@types/node is pinned to the major version matching the Node runtime (24). Auto-updating",
      "to 25 pulls in type definitions for APIs not available on the target runtime, which can",
      "mask compatibility issues. Re-evaluate when the project moves to the next even-numbered Node LTS.",
      "all three @analogjs/* packages (vite-plugin-angular, vitest-angular, storybook-angular) are",
      "pinned without ^ because minor releases (2.4+, 2.5+) introduce stricter peer-dep ranges that",
      "require Angular patch versions ahead of the ^21.2.6 we depend on, forcing --legacy-peer-deps",
      "and pulling in vite 8 (which has a high-severity path-traversal CVE). Hold at 2.3.1 until the",
      "toolchain stabilises; re-evaluate when @analogjs publishes a release that respects ^21 peers."
    ]
  },
  "peerDependencies": {
    "@angular/cdk": ">=21.0.0",
    "@angular/core": ">=21.0.0",
    "@angular/material": ">=21.0.0",
    "@teqbench/tbx-mat-icons": ">=4.2.3",
    "@teqbench/tbx-mat-severity-theme": ">=8.0.5"
  },
  "devDependencies": {
    "@analogjs/storybook-angular": "2.7.1",
    "@analogjs/vite-plugin-angular": "2.6.0",
    "@analogjs/vitest-angular": "2.6.0",
    "@angular/animations": "^21.2.6",
    "@angular/build": "^21.2.4",
    "@angular/cdk": "^21.2.4",
    "@angular/common": "^21.2.6",
    "@angular/compiler": "^21.2.6",
    "@angular/compiler-cli": "^21.2.6",
    "@angular/core": "^21.2.6",
    "@angular/forms": "^21.2.6",
    "@angular/material": "^21.2.4",
    "@angular/platform-browser": "^21.2.6",
    "@angular/platform-browser-dynamic": "^21.2.6",
    "@storybook/angular": "^10.3.3",
    "@teqbench/tbx-mat-icons": "^4.2.0",
    "@teqbench/tbx-mat-severity-theme": "^8.0.2",
    "@types/node": "~24.13.0",
    "@vitest/coverage-v8": "^4.1.1",
    "angular-eslint": "^21.3.1",
    "eslint": "^9.39.2",
    "eslint-plugin-tsdoc": "^0.5.2",
    "husky": "^9.1.7",
    "jsdom": "^29.0.1",
    "lint-staged": "^17.0.0",
    "ng-packagr": "^21.2.2",
    "prettier": "^3.8.1",
    "storybook": "^10.3.3",
    "typescript": "~5.9.0",
    "typescript-eslint": "8.60.1",
    "vitest": "^4.0.18",
    "zone.js": "^0.16.1"
  },
  "overrides": {
    "@angular/build": {
      "vite": "7.3.2"
    }
  },
  "overridesPinned": {
    "//": [
      "vite is pinned to 7.3.2 inside @angular/build's subtree because @angular/build bundles",
      "vite internally but does not constrain the version tightly enough; vite 7.4+ causes",
      "build failures with ng-packagr. Remove this override when @angular/build supports",
      "vite 7.4+."
    ]
  },
  "lint-staged": {
    "*.{ts,json,md,yml,yaml,scss,css}": [
      "prettier --write"
    ]
  }
}
